<form class="ui form" action="{{ route('comments.store', { postid: post.id}) }}" method="post">
    {{ csrfField() }}
    @!input({label: 'Your name', name: 'poster', size: '20', wide: 'two'})
    @!textarea({label: 'Comment', name: 'comment', rows:'5', cols: '50'})
    @!input({type:'submit',value:'Submit'})
</form>


